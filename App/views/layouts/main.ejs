<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Event Reminder App</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Tailwind -->
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/css/style.css">

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

  <style>
    body { font-family: 'Inter', sans-serif; }
    .shadow-soft { box-shadow: 0 6px 18px rgba(0,0,0,0.08); }
    .transition-all { transition: all 0.3s ease-in-out; }
    .flash-enter { opacity: 0; transform: translateY(-10px); animation: fadeSlideIn 0.4s forwards; }
    @keyframes fadeSlideIn { to { opacity: 1; transform: translateY(0); } }
    .card:hover { transform: translateY(-3px); box-shadow: 0 12px 20px rgba(0,0,0,0.15); }

    /* Utility for dynamic event color (example usage) */
    .btn-event { transition: all 0.3s ease-in-out; color: white; font-weight: 500; }
  </style>
</head>

<body class="bg-gray-50 min-h-screen flex flex-col">

  <!-- Navbar -->
  <nav class="bg-gradient-to-r from-purple-600 via-pink-600 to-red-500 shadow-soft sticky top-0 z-50 transition-all">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex justify-between items-center h-16">

      <!-- Logo -->
      <h1 class="text-white text-2xl font-bold tracking-wide hover:text-yellow-400 transition-all cursor-pointer">
        EventReminder
      </h1>

      <!-- Links -->
      <div class="flex items-center space-x-4">
        <% if(user){ %>
          <span class="text-gray-100">Hi, <span class="font-semibold text-white"><%= user.name %></span></span>
          <a href="/dashboard" class="bg-purple-700 hover:bg-purple-800 text-white px-4 py-2 rounded-lg shadow-soft transition-all">Dashboard</a>
          <a href="/logout" class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-lg shadow-soft transition-all">Logout</a>
        <% } else { %>
          <a href="/login" class="bg-gray-200 hover:bg-gray-300 text-gray-800 px-4 py-2 rounded-lg transition-all">Login</a>
          <a href="/register" class="bg-gray-200 hover:bg-gray-300 text-gray-800 px-4 py-2 rounded-lg transition-all">Register</a>
        <% } %>
      </div>
    </div>
  </nav>

  <!-- Main Content -->
  <main class="container mx-auto mt-8 px-4 flex-1">

    <!-- Flash Messages -->
    <% if(success_msg && success_msg.length > 0){ %>
      <div class="flash-enter bg-green-50 border-l-4 border-green-400 text-green-800 p-4 rounded-lg mb-6 shadow-soft relative">
        <div class="flex justify-between items-center">
          <p class="font-medium"><%= success_msg %></p>
          <button onclick="this.parentElement.parentElement.remove()" class="text-green-600 hover:text-green-800">✖</button>
        </div>
      </div>
    <% } %>

    <% if(error_msg && error_msg.length > 0){ %>
      <div class="flash-enter bg-red-50 border-l-4 border-red-400 text-red-800 p-4 rounded-lg mb-6 shadow-soft relative">
        <div class="flex justify-between items-center">
          <p class="font-medium"><%= error_msg %></p>
          <button onclick="this.parentElement.parentElement.remove()" class="text-red-600 hover:text-red-800">✖</button>
        </div>
      </div>
    <% } %>

    <!-- Page Content Card -->
    <div class="bg-white p-6 rounded-2xl shadow-soft">
      <%- body %>
    </div>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-900 text-gray-300 mt-8 py-4 text-center text-sm">
    © <%= new Date().getFullYear() %> EventReminder. All rights reserved.
  </footer>

</body>
</html>
